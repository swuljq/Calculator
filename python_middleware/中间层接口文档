接口文档：中间端
接口基本信息

1.接口名称：中间层接口

2.接口路径：`/compute` 
 
3.请求方法：`POST`  

4.请求类型：`application/json`

5.接口功能：接收客户端JSON形式表达式，对客户端传递过来的中缀表达式判断格式是否正确，如果正确，转换为后缀表达式(逆波兰表示法)，再封装成JSON格式，向计算层服务 `http://localhost:8090/ compute ` 发送 POST 请求，之后获取返回结果，再转回客户端。如果错误，将错误信息返回给客户端

6.处理逻辑
1）接收客户端JSON形式表达式

  /*示例
    json
    {
      "expression": "sin(30)+2^(-9)”
    }

2）解析判断处理表达式

①	识别和处理像 sin、cos、tan等数学函数以及运算符号、数字
②	确保括号的正确配对
③	去除空格和无效字符
④	转换为后缀表达式(逆波兰表示法)

如果算式正确，则将JSON形式逆波兰表示法表达式发送给计算层
如果算式错误，不发送给计算层，并将错误信息发送给客户端

  /*示例
    json
    {
      "error": "表达式错误"
    }

3）向计算端发送JSON形式

  /*示例
  客户端发送JSON{"expression": "sin(30)+2^3 "}

  向计算端发送JSON{"expression": "sin" ,2 ,3 , " ^", " +"}

4）接收计算结果

  json
  {
    "success": "***",
  "result": ***,
  "error": ***
  }
说明：
参数名	是否必填	说明
success	是	请求是否成功，true或false
result	是	计算结果，成功为数值，失败为null
error	是	错误信息，失败时为错误描述，成功为null

4）向客户端返回计算结果

  计算成功返回示例
    json
    {
      "result": 5.0
    }

  计算错误返回示例
    json
    {
      "error": "错误原因"
    }
